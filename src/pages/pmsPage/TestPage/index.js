import React, { useEffect, useState, useRef } from 'react';
import { Button, message } from 'antd';
import moment from 'moment';

export default function TestPage(props) {
  const {} = props;
  useEffect(() => {
    return () => {};
  }, []);
  return <div className="example-box">TestPage</div>;
}